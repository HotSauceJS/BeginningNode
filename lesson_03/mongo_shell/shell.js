// mongo shell is just like node shell
// launch without db mongo -nodb

var a = 1
var b = "Hello"

print(b)

var list = [1,2,3];
var listing = { title: "Vintage Bike", description: "Awesome 70s Schwinn" }

function add (x, y) { return x + y }

// ctrl + c to exit
